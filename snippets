import * as React from 'react';
import * as RN from 'react-native';

import * as NB from 'native-base';

interface Props {}

interface State {}

export default function LoginPage(props: Props) {
  return <NB.Container style={styles.container}>

  </NB.Container>;
}

const styles = RN.StyleSheet.create({
  container: {},
  content: {},
  contentContainerStyle: {},
});



import * as React from 'react';
import * as RN from 'react-native';

import { BodyIntroText } from '../styles/fontSize';

interface Props {
  title: string;
  onPress: () => void;
  containerStyle?: RN.StyleProp<RN.PressableProps>;
}

interface State {}

export default function ButtonPrimaryBig(props: Props) {
  return (
    <RN.Pressable style={[styles.container, props.containerStyle]}>
      <RN.Text style={[BodyIntroText, styles.title]}>Login</RN.Text>
    </RN.Pressable>
  );
}

const styles = RN.StyleSheet.create({
  container: {},
  title: {},
});

fill=${
    props.fill ?? '#000000'
  }/>